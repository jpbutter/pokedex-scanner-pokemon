# Security Policy

Only the latest minor version is supported while the project is experimental. Report vulnerabilities with a private GitHub security advisory where possible.

Version 0.1 stores no requests. Future image adapters must document accepted types, size limits, retention and deletion before being enabled by default.